I would use linked list when I need to frequently insert or delete elements without shifting other element. It provide a easier way to instert and remove because elements are connected by pointers rather than storage.
Stack remove the last item added, while queue remove the first added.
A stack could be useful to undo an action if you accidentally add something that you do not want.
Array is used when you know the size and need fast access by position.

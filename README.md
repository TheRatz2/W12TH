1. I would use linked list when I need to frequently insert or delete elements without shifting other element. It provide a easier way to instert and remove because elements are connected by pointers rather than 
   storage.
2. Stack remove the last item added, while queue remove the first added.
3. A stack could be useful to undo an action if you accidentally add something that you do not want.
4. Array is used when you know the size and need fast access by position.
